
version: 1.0
runtime: python3

build:
  commands:
    build:
      - pip install -r requirements.txt

run:
  command: gunicorn app:app
  network:
    port: 8000
  env:
    - name: FAUNA_SECRET_PARAMETER
      value: fauna-secret